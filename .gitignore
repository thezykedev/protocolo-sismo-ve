# build output
dist/
build/
.svelte-kit/

# generated types
.astro/
.wrangler/
.vercel/

# dependencies
node_modules/
test-results/
playwright-report/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.local
.env.production
.env.ci

# pocketbase local runtime (binario descargado + datos locales)
infra/pocketbase/bin/
infra/pocketbase/pb_data/
infra/pocketbase/pb_public/

# snapshot SQLite de respaldo (generado) + staging de ingestión (datos sensibles)
apps/ayuda/.fallback/
data/ingest/staging.db

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# local research and source captures
docs/research/

# local MCP server config (may contain credentials)
.mcp.json
data/drive_data/
# ingest ledger + transcribed patient data (sensitive, local-only)
data/ingest/
